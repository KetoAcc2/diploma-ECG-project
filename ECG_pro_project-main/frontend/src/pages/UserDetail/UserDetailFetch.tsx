import GetUserDetailCard from "../../components/GetUserDetailCard";

const UserDetailFetch = () => {
  return (
    <div>
      <GetUserDetailCard style={{display:'flex'}}/>
    </div>
  );
};

export default UserDetailFetch;
